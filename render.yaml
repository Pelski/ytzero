services:
  - type: web
    name: ytzero
    runtime: docker
    repo: https://github.com/Pelski/ytzero
    dockerfilePath: ./Dockerfile
    plan: starter
    autoDeploy: false
    healthCheckPath: /api/health
    envVars:
      - key: PORT
        value: "3001"
      - key: YTZERO_AUTH_METHOD
        value: shared
      - key: YTZERO_AUTH_PASSWORD
        sync: false
    disk:
      name: ytzero-data
      mountPath: /data
      sizeGB: 10
